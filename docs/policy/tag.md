# [정책] 태그

## 1. 태그 생성
- 사용자는 링크에 태그를 추가할 수 있다.
- 링크당 최대 10개의 태그를 추가할 수 있다.
- 태그명은 최대 20자까지 입력할 수 있다.
- 시스템은 저장된 링크의 내용을 기반으로 태그를 자동 생성할 수 있다.

## 2. 태그 수정
- 사용자는 직접 생성한 태그를 수정할 수 있다.
- 사용자는 자동 생성된 태그를 수정하거나 삭제할 수 있다.

## 3. 태그 삭제
- 태그 삭제 시 해당 링크의 태그만 제거된다.
- 다른 링크의 동일한 태그에는 영향을 주지 않는다.
