# Error Code Policy

`errorCode`는 `number`로 관리한다.

형식은 `[9][도메인(2자리)][순번(3자리)]`이다.

- `9`: HTTP status code와 구분하기 위한 비즈니스 에러 식별자
- `도메인`: 기능 영역 구분
- `순번`: 도메인 안에서의 에러 순번, `001`부터 시작

## 도메인 코드

| 도메인   | 코드 |                범위 |
| -------- | ---: | ------------------: |
| `COMMON` | `10` | `910001` ~ `910999` |
| `FOLDER` | `20` | `920001` ~ `920999` |
| `LINK`   | `30` | `930001` ~ `930999` |
| `IMAGE`  | `40` | `940001` ~ `940999` |
| `AUTH`   | `50` | `950001` ~ `950999` |
| `USER`   | `60` | `960001` ~ `960999` |

## 에러 코드와 응답 메시지

### COMMON

| errorCode | message                           |
| --------: | --------------------------------- |
|  `910001` | `{필드 경로}: {검증 실패 메시지}` |
|  `910002` | `서버 내부 오류가 발생했습니다.`  |

`910001`의 `message`는 Zod 검증 결과를 조합해 동적으로 반환한다. 여러 필드가 실패하면 `, `로 연결한다.

### FOLDER

| errorCode | message                          |
| --------: | -------------------------------- |
|  `920001` | `폴더를 찾을 수 없습니다.`       |
|  `920002` | `이미 존재하는 폴더 이름입니다.` |

### LINK

| errorCode | message                                      |
| --------: | -------------------------------------------- |
|  `930001` | `링크를 찾을 수 없습니다.`                   |
|  `930002` | `삭제된 링크가 아니므로 복구할 수 없습니다.` |
|  `930003` | `이미 저장한 링크입니다.`                    |

### IMAGE

| errorCode | message                                           |
| --------: | ------------------------------------------------- |
|  `940001` | `이미지 색상을 추출할 수 없습니다.`               |
|  `940001` | `이미지 색상 팔레트를 추출할 수 없습니다.`        |
|  `940001` | `sharp로 이미지 색상을 추출할 수 없습니다.`       |
|  `940002` | `이미지 URL 요청에 실패했습니다.`                 |
|  `940002` | `이미지 요청이 정상적으로 처리되지 않았습니다.`   |
|  `940002` | `이미지 URL 리다이렉트 횟수가 너무 많습니다.`     |
|  `940002` | `이미지 URL 리다이렉트 처리에 실패했습니다.`      |
|  `940002` | `이미지 URL 리다이렉트 위치가 없습니다.`          |
|  `940002` | `이미지 응답이 비어 있습니다.`                    |
|  `940003` | `이미지 요청 시간이 초과되었습니다.`              |
|  `940004` | `이미지 크기가 제한을 초과했습니다.`              |
|  `940005` | `이미지 응답의 Content-Type이 지원되지 않습니다.` |

### AUTH

| errorCode | message                                   |
| --------: | ----------------------------------------- |
|  `950001` | `유효하지 않은 토큰입니다.`               |
|  `950002` | `만료된 토큰입니다.`                      |
|  `950003` | `소셜 ID 토큰 검증에 실패했습니다.`       |
|  `950004` | `지원하지 않는 소셜 로그인 제공자입니다.` |

### USER

| errorCode | message                    |
| --------: | -------------------------- |
|  `960001` | `유저를 찾을 수 없습니다.` |

## 추가 규칙

- 새 코드는 해당 도메인의 마지막 순번 다음 값을 사용한다.
- 같은 원인으로 발생하는 세부 상황은 동일한 `errorCode`를 사용할 수 있다.
- 서로 다른 클라이언트 처리가 필요하면 별도의 순번을 부여한다.
- 명시적인 `errorCode`가 없는 Nest 기본 `HttpException`은 `910002`를 사용한다.
